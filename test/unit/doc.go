// Package unit is reserved for additional cross-package unit tests that do
// not belong next to a single package. Package-level tests live next to the
// code they cover, so this directory is normally empty.
package unit
